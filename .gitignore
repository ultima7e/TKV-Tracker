node_modules/
.superpowers/
.claude/settings.local.json
.vercel/
.env
.env.local

# Real project workbooks stay out of version control (privacy + size).
# Only the synthetic dev sample is tracked.
*.xlsx
!data/sample.xlsx

# Large CAD source files + conversion temp artifacts (the compact
# public/assets/tunnels.bin built from them IS tracked).
*.dwg
*.dxf
*.pptx
*.xer
_*.scr
_acad_log.txt

# P6 schedule source files (the parsed data is served from Nutstore)
*.xer

# Local dev user store (production uses Vercel KV / Upstash)
data/.users.json
data/.tkv_*.json
